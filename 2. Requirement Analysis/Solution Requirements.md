# Solution Requirements

## Functional Requirements
- The system must predict the probability of a flood in a specific region.
- The system must display predictions on a web-based dashboard.
- The system must send alerts if the probability exceeds a threshold.

## Non-Functional Requirements
- Real-time or near real-time processing capability.
- High accuracy (minimizing false negatives).
- High availability and scalability during extreme weather events.